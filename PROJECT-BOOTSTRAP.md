# PROJECT-BOOTSTRAP.md

تمهيدٌ ثابتٌ لأيّ وكيلٍ آليّ يعمل على هذا المستودع — أيّ حساب Claude، أو ChatGPT/Codex، أو غيره. اقرأ هذا الملفّ أوّلاً، دائماً، قبل أيّ عمل. مصدر الحقيقة الوحيد هو حالة GitHub نفسها (التذاكر، الـPRs، الفروع)، لا ذاكرة جلسةٍ أو حسابٍ بعينه.

## دور الوكيل

وكيلٌ سحابيٌّ يعمل على هذا المستودع — نسخة الطلّاب المنشورة من دورة التحضير لامتحان المعرفة في التمريض (Kenntnisprüfung Pflege) — بلا سطح مكتبٍ ولا ملفّاتٍ محليّة. يعمل بشكلٍ مستقلٍّ على المهامّ القابلة للتنفيذ سحابيّاً فقط: كود الموقع، الاختبارات، GitHub Actions، الفروع وطلبات السحب (PRs)، مراجعات Codex عبر `@codex review`، وتوثيق حال الدروس/الأفلام داخل هذا المستودع.

## حقائق يجب احترامها

- هذا المستودع **عامّ (Public)** ويُنشر عبر GitHub Pages من `main` (الجذر). **الدمج في `main` يعني نشراً فوريّاً على الموقع العامّ.**
- هذا مخرَج بناء: `PAKET.json` يسمّي Commit المصدر في المستودع الخاصّ `mohamedasaad971-pixel/togetherwecan-pflegekurs`، غير المتاح هنا. `app.js` هنا نسخةٌ أقدم من ذلك المصدر. أيّ إصلاحٍ كوديٍّ هنا يُمحى بالبناء التالي ما لم يُنقَل إلى المصدر أيضاً. لذا كلّ PR يُغيّر كوداً يجب أن يحوي السطر «يحتاج نقلاً إلى المصدر» مع قائمة السلوك المتغيّر، ويُضاف إلى جدول الملكيّة في #2. نقل الإصلاحات إلى المصدر مسؤوليّة الجلسة المحلّيّة، لا الجولة السحابيّة.
- ممنوعٌ كتابة أيّ تفاصيل داخليّة في هذا المستودع العامّ (issues/PRs/commits/docs): لا رسائل خطأٍ من مزوّدين، لا معرّفات API/أصول/مهامّ، لا تفاصيل مراجعةٍ سريريّة، لا بيانات شخصيّة. حال الأفلام/الدروس هنا يقتصر على: المالك + الفرع + حالٌ عامٌّ مختصرٌ مثل «متوقّف — مانعٌ خارجيّ».
- لا تغييرَ في محتوًى طبّيٍّ/تعليميٍّ، ولا إضافة/استبدال وسائط، ولا تغييرَ في `robots.txt`/الفهرسة، ولا أيّ ادّعاء اعتماد سريريّ من أيّ نوع. لا توجد مراجعةٌ سريريّةٌ بشريّةٌ؛ المحتوى يبقى غير معتمَد.

## ترتيب الأولويّات

المصدر الوحيد للأولويّة الحاليّة هو **جدول الملكيّة في التذكرة #2** — راجعه أوّلاً دائماً. الترتيب العامّ المتّبع تاريخيّاً عند عدم وجود توجيهٍ أحدث في #2:

1. CI لهذا المستودع (GitHub Actions) أوّلاً.
2. حين يصبح CI أخضر على `main`: التعليق على الـPRs المفتوحة الأخرى للإعلام فقط، دون تعديل عمل أصحابها.
3. تدقيق سهولة الوصول، وتخطيط الجوّال (375px بلا تمريرٍ أفقيّ)، والروابط المعطوبة، وأخطاء التشغيل — تذكرةٌ واحدةٌ لكلّ عيبٍ حقيقيٍّ بدليل، ثمّ إصلاحٌ بـPR واحدٍ تلو الآخر. تخطَّ أيّ بندٍ له مالكٌ بالفعل.
4. الحفاظ على `STATUS.md` (عربيّ): حال الدروس/الأفلام — مالكٌ وحالٌ مختصرٌ فقط، حسب القاعدة أعلاه.

## سياسة الفروع وPRs

- **مالكٌ واحدٌ لكلّ بندٍ.** فروع `codex/*` وما يخصّها من PRs/issues مِلكُ وكيل Codex السحابيّ (ChatGPT). **إن لم تكن Codex نفسَه:** لا تدفع إليها، ولا تُعدّلها، ولا تدمجها — اقرأها وعلّق عليها للتنسيق فقط. **وإن كنت Codex:** هذا القيد لا يخصّ فروعك أنت؛ اعمل عليها كالمعتاد. فروع الوكيل الآخر (Claude أو غيره) هي `claude/<item>` أو ما يكافئها، بنفس المبدأ: كلّ وكيلٍ يملك فروعه هو، لا فروع غيره.
- قبل بدء أيّ بندٍ: اطلبه بتعليقٍ عربيٍّ مختصرٍ في #2 (البند، المالك، الفرع). إن كان له مالكٌ بالفعل أو PR مفتوحٌ يغطّيه، لا تُكرَّر العمل — تابَع من ذلك العمل فقط إن كان مِلكك.
- فرعٌ مخصّصٌ واحدٌ وPR واحدٌ لكلّ إصلاحٍ، من `main` الحاليّ. أصغر إصلاحٍ كاملٍ + حارس انحدارٍ ذو معنى. وصف PR بالعربيّة: السلوك، التحقّق، «يحتاج نقلاً إلى المصدر» عند تغيّر الكود.
- علّق `@codex review` بعد كلّ دفعةٍ تستحقّ مراجعة. عالج كلّ ملاحظةٍ فعليّةٍ من Codex، ثمّ ادفع واطلب مراجعةً ثانية. **لا تُكرَّر طلبات `@codex review` أو الدفعات كلّ دقائقَ قليلة** — انتظر ردّاً واضحاً (نتيجة مراجعةٍ، أو نجاح/فشل CI) قبل الجولة التالية، لتفادي جلساتٍ متعدّدةٍ تتنافس على الفرع نفسه.
- **بوابة الدمج (الكلّ مطلوبٌ، على Commit الرأس الحاليّ بالذات):**
  1. موافقة Codex الصريحة، أو تقريرٌ بلا ملاحظاتٍ فعليّةٍ على هذا الـCommit بالذات؛
  2. وجود تشغيلٍ واحدٍ على الأقلّ لفحوصات GitHub Actions على هذا الـCommit، وكلّها بحالة `completed` ونتيجة `success` — صفر تشغيلاتٍ، أو `queued`/`in_progress`/`pending`/`skipped`/`neutral`، تعني جميعها **غير قابلٍ للدمج**، لا "أخضر بالتغاضي"؛
  3. GitHub يُقرّر MERGEABLE/CLEAN؛
  4. لا ملاحظات مراجعةٍ معلّقةٍ غير محلولة.

  السكوت ليس موافقةً أبداً. لا تُرحَّل نتائج موافقةٍ أو اختبارٍ من Commit أقدم. لا تُستبدَل نتيجة GitHub Actions باختبارٍ محلّيّ.

  إن لم يردّ Codex، أو بقيت فحوصات Actions عالقةً أو لا تعمل، أو منع قرارٌ خارجيّ التقدّمَ الآمن: اترك تعليقاً عربيّاً واحداً واضحاً يسمّي المانع في #2 (لا تُكرّره كلّ جولة) وانتقل إلى البند التالي القابل للتنفيذ سحابيّاً دون دمج.

  ادمج (Squash، مع حذف الفرع) فقط الـPRs الخاصّة بك.
- بعد كلّ دمجٍ: حدّث #2 وابدأ البندَ التالي تلقائيّاً.

## الملفّات المرجعيّة الواجب قراءتها أوّلاً (بهذا الترتيب)

1. هذا الملفّ (`PROJECT-BOOTSTRAP.md`).
2. التذكرة #2 (جدول الملكيّة) — بما فيها كلّ التعليقات.
3. كلّ الـPRs المفتوحة والمُغلقة حديثاً، وكلّ الفروع.
4. أيّ issues أخرى مفتوحة.
5. `STATUS.md` إن وُجد.
6. `PAKET.json` لفهم علاقة هذا المستودع بمستودع المصدر الخاصّ.

## ما يجب عدم فعله

- لا تكشف، أو تطلب، أو تطبع أسراراً (رموزاً/مفاتيحَ/بيانات اعتمادٍ) أبداً.
- لا تُغيّر محتوًى طبّيّاً أو تعليميّاً، ولا تُضِف أو تستبدل وسائط، ولا تلمس `robots.txt`/الفهرسة، ولا تدّعِ أو تُلمِّح اعتماداً سريريّاً من أيّ نوع.
- لا تستخدم أيّ خدمةٍ مدفوعةٍ (توليد صورٍ/صوتٍ/فيديو) ولا ترفع صوراً أو بياناتٍ دون موافقةٍ صريحةٍ حديثةٍ من صاحب المستودع تذكر العدد والتكلفة. نجاح مزوّدٍ في تقريره ليس مراجعةً بصريّة.
- لا تدمج، ولا تُعدّل، ولا تدفع إلى فرعٍ أو PR يملكه وكيلٌ آخر غيرك (فروع `codex/*` مثالٌ نموذجيٌّ لذلك من منظور أيّ وكيلٍ ليس Codex؛ هذا لا يمنع وكيل Codex من العمل على فروعه هو).
- لا تدمج أيّ PR ما لم تتحقّق بوابة الدمج كاملةً على Commit الرأس بالذات.
- لا تعمل على بندٍ له مالكٌ آخر مُعلَنٌ في #2.
- لا تفترض توفّر ملفّاتٍ محلّيّةٍ (أفلامٍ/أصواتٍ/صورٍ على جهاز صاحب المستودع) — سجِّل مانعاً في #2 وانتقل.
- لا تُعِد تقديم أيّ مهمّة توليد وسائط فشلت سابقاً دون طلبٍ صريحٍ جديد.

## قاعدة المستودع العامّ

إذا كان المستودع Public، فلا تُنشأ أو تُحدّث ملفات تحتوي معلومات داخلية أو تشغيلية أو خاصة بالمطورين. استخدم إحالات إلى المستودع المركزي الخاص عند الحاجة.

## المحتوى يُصحَّح في المصدر لا هنا

ملفّات الموقع في هذا المستودع (`index.html`، ملفّات `*.js`، `stil.css`، `medien/`، `robots.txt`، `PAKET.json`) **مخرَجُ بناءٍ** من مستودع المصدر، ويُكتب فوقها — أو تُحذف — مع كلّ بناءٍ جديد. أيّ تصحيحٍ في المحتوى أو النصوص يُقدَّم في المصدر، لا هنا. وما يُصلَح هنا في الكود مؤقّتٌ حتى يُنقَل إلى المصدر (انظر «يحتاج نقلاً إلى المصدر» أعلاه).

## تسلسل البدء (Bootstrap Sequence)

1. اقرأ هذا الملفّ، ثمّ [`PROJECT-STATUS.md`](./PROJECT-STATUS.md)، ثمّ [`DECISIONS.md`](./DECISIONS.md).
2. ثمّ `README.md`، والتذكرة #2 وبقيّة الـissues المفتوحة، وكلّ الـPRs المفتوحة والمدموجة حديثاً، وحال `main` الأخيرة (`git fetch` ثمّ `git log origin/main`).
3. افهم الحال من GitHub وحده، لا من ذاكرة جلسةٍ سابقة.
4. اختر بنداً بلا مالك، واطلبه في #2، ثمّ افتح له فرعاً وPR من `main` الحاليّ.

## معايير الدمج في `main`

هي «بوابة الدمج» في قسم «سياسة الفروع وPRs» أعلاه، كاملةً وعلى Commit الرأس بالذات. ولأنّ الدمج في `main` نشرٌ فوريٌّ، لا استثناء منها.

## التسليم بين الوكلاء

- كلّ ما يلزم من بعدك يبقى على GitHub: لا حال محلّيّة فقط.
- عند التوقّف اترك تعليقاً على الـPR يذكر Commit الرأس (المختصر)، وما أُنجز، وما بقي، وما هو متوقّفٌ ولماذا.
- حدِّث سطر البند في #2 (المالك، الفرع، الحال).

---

هذا الملفّ يُبقي أيّ وكيلٍ — أيّ حساب Claude، أو ChatGPT/Codex، أو غيره — يبدأ من القواعد نفسها وحال GitHub نفسه كمصدر وحيدٍ للحقيقة، بلا اعتمادٍ على ذاكرة جلسةٍ أو حسابٍ بعينه. حدِّثه عند تغيّر القواعد الفعليّة، عبر PR كأيّ تغييرٍ آخر في هذا المستودع.
